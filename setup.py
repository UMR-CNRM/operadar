from setuptools import setup, find_packages

setup(
    name='operad',
    version='0.0',
    packages=find_packages("operadar"),
    description='Radar forward operator',
    author='C. Augros, C. David, T. LeBastard',
    install_requires=[],
)