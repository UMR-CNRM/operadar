# This is the Makefile for TmatInt program

# Compiler settings are here
#include ./makefile_include

# Objects to compile
OBJECTS = ./TmatInt.f90

CF = gfortran

CFLAGS = -pipe -Wall 

all: TmatInt

TmatInt : $(OBJECTS) 
	$(CF) $(CFLAGS) $(OBJECTS) $(LIBS) -o ./TmatInt 

clean:
	rm -f *.o TmatInt
