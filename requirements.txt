# Minimal pip dependencies (pin after CI verification)
numpy==2.3.5
xarray>=2023.2
pandas>=1.5
netCDF4>=1.6
matplotlib>=3.6
cartopy>=0.21
dask>=2023.2
scipy>=1.10
tqdm>=4.65
pyyaml>=6.0

# epygram must be installed from pip (installed via conda env's pip step below)
epygram
